var x = 5;
var y = 7;

print("x: " + x);
print("y: " + y);

print("If Statement");
var b0 = x == y || x < y;
if (b0) {
    print("(b0) true");
} else {
    print("(b0) false");
}

print("Variable assignment");
var b1 = x < y && y == 6;
var state = !b1;
print("state: " + state);

print("Arithmetic operations");
print("!(5 * (y >= x) && 0): " + (!(5 * (y >= x) && 0)));

print("Parameter input");
func printIf($number: cond, $text: str) -> $void {
    if (cond == 1) {
        print(str);
    }
}

printIf(!(x == 4), "!(x == 4)");
printIf(!(x == 5), "!(x == 5)");
printIf(!(x == 6), "!(x == 6)");
